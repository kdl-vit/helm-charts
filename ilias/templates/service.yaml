apiVersion: v1
kind: Service
metadata:
  name: {{ include "ilias.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "ilias.name" . }}

