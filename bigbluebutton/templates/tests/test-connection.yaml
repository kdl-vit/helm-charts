apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bigbluebutton.fullname" . }}-test-connection"
  labels:
    {{- include "bigbluebutton.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bigbluebutton.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
