apiVersion: v1
kind: Service
metadata:
  name: {{ include "bigbluebutton.fullname" . }}
  labels:
    {{- include "bigbluebutton.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bigbluebutton.selectorLabels" . | nindent 4 }}
