apiVersion: v1
kind: Service
metadata:
  name: {{ include "academyfive.fullname" . }}
  labels:
    {{- include "academyfive.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "academyfive.selectorLabels" . | nindent 4 }}
